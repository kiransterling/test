1. Get the Argo Workflows UI URL by running these commands:
  export NAMESPACE={{ .Release.Namespace }}
  export RELEASE_NAME={{ .Release.Name }}
  echo "Visit http://127.0.0.1:2746 to access Argo Workflows"
  kubectl -n ${NAMESPACE} port-forward svc/${RELEASE_NAME}-argo-workflows-server 2746:2746
